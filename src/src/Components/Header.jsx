/**
 * Creates the app header.
 */
const Header = () => {
  return (
    <header className="app-header">
      <h1 className="app-header-logo">Pilot Alex Flight Tracker</h1>
    </header>
  );
};

export default Header;
